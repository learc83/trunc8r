sudo docker-compose build
sudo docker-compose build <service_name>
sudo docker-compose up
sudo docker-compose up -d <service_name> # detached--in the background

sudo docker-compose run --rm --service-ports ruby_dev


sudo docker container ls

docker exec -it <CONTAINER ID> bash

sudo docker-compose -f docker-compose.production.yml -p production up --build --force-recreate

sudo docker system prune -a

sudo docker volume rm $(sudo docker volume ls -q)